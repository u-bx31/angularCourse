import { EmailValidaterDirective } from './email-validater.directive';

describe('EmailValidaterDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailValidaterDirective();
    expect(directive).toBeTruthy();
  });
});
